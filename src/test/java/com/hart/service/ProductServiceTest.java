package com.hart.service;


import org.springframework.boot.test.context.SpringBootTest;


@SpringBootTest
public class ProductServiceTest {
    
	/*
	 * @Autowired private ProductService productService;
	 * 
	 * @Test public void testProductListIntegration() { int pcno = 100574;
	 * List<ProductlistintegVO> productList = productService.Productlistinteg(pcno);
	 * 
	 * // assert that the productList is not null and has the expected size
	 * assertNotNull(productList); assertEquals(1, productList.size());
	 * 
	 * ProductlistintegVO productlistintegVO = productList.get(0); List<ProductsVO>
	 * productlist = productlistintegVO.getProductlist(); List<ProductsidesamllVO>
	 * smallcategory = productlistintegVO.getSmallcategory();
	 * 
	 * // assert that the productlist and smallcategory lists are not null and have
	 * the expected sizes assertNotNull(productlist); assertEquals(10,
	 * productlist.size()); assertNotNull(smallcategory); assertEquals(5,
	 * smallcategory.size()); }
	 */
    
}
